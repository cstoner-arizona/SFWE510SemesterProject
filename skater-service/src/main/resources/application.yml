spring:
  application:
    name: skater-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  config:
    import: "optional:configserver:http://configserver:8089"

server:
  port: ${PORT:8081}
