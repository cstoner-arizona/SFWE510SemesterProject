apiVersion: v1
kind: Service
metadata:
  name: logstash
  namespace: skate-spots
spec:
  type: ClusterIP
  selector:
    app: logstash-logstash
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: tcp-input
